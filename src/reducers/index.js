import { combineReducers } from "redux";

const template = (state = [], action) => {
   switch (action.type) {
      default:
         return state;
   }
};

export default combineReducers({ template });
